import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#A57EFA',
        },
        secondary: {
            main: '#FFC107',
        },
        //TODO: Add more color definitions as needed
    },
});

export default theme;